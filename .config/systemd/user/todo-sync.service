[Unit]
Description=Todo git repo synchronization service

[Service]
Environment=SSH_AUTH_SOCK=%t/gnupg/S.gpg-agent.ssh
Type=oneshot
ExecStartPre=/usr/bin/gpg --card-status
ExecStart=bash -c %h/scripts/todo-sync.sh
