[Unit]
Description=RClone mount of Google Drive to ~/files

[Service]
Type=notify
ExecStart=/usr/bin/rclone mount gdrive:/ %h/files/ --vfs-cache-mode full --config %h/.config/rclone/rclone.conf --cache-dir %h/.cache/rclone

[Install]
WantedBy=default.target
